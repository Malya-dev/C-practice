using System;
using System.Collections.Generic;

namespace evaluation
{
    public class program
    {
        static void Main(string[] args)
        {
            var num=new enums();
            num.numenum();

            // var mstr=new string_methods();
            // mstr.showstring();

            // var demo=new demo_string_builder();
            // demo.stringshow();


            // var jag=new Jagged();
            // jag.jarray();

            // var Arrmthds=new arrmthds();
            // Arrmthds.methodarray();

            // var multi=new multidimension();
            // multi.twodarray();
          
            // var dsum=new sumarray();
            // dsum.summation();

            // var rvrs=new reverse();
            // rvrs.rev();

            // var maximum=new maxim();
            // maximum.largest();

            // var Del=new delete_array();
            // Del.del();

          
        }
      
    }

}